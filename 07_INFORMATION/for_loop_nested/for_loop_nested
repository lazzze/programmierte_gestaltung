var colorValue = 0;

function setup() {
  createCanvas(800, 800);
  background(0);
  fill(255);
  noStroke();
}

function draw() {
  for (y = 10; y < 255; y += 10) {
    for (x = 10; x < 255; x += 10) {
      fill(x, y, mouseX);
      ellipse(x, y, 8, 8);
    }
  }
}
