var colorValue = 0;

function setup() {
  createCanvas(800, 800);
  background(0);
  fill(255);
  noStroke();
}

function draw() {
  for (i = 100; i < 400; i += 50) {
    ellipse(i, 100, 20, 20);
  }
  fill(colorValue);
  colorValue += 1;
}
